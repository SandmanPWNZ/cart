import React from 'react';
import {BrowserRouter as Link} from "react-router-dom";

const Home = () => (
    <div>
        jopa
        <Link to="/cart">Cart</Link>
    </div>
);

export default Home;